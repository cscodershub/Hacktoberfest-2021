int Solution::removeDuplicates(vector<int> &A) {
    int i = 0;
    int n = A.size();

    for(int j = 1; j < n; j++){
        if(A[j-1] != A[j]){
            A[i++] = A[j-1];
        }
    }

    A[i++] = A[n-1];

    return i;
}
